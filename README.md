# NoFAT Telegram Bot (MVP)

Минимальный каркас Telegram-бота на **Python + aiogram** для запуска MVP.

## Быстрый старт (локально)

1. Установите зависимости:
   ```bash
   python -m venv .venv
   source .venv/bin/activate
   pip install -r requirements.txt

